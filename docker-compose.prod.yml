version: "3"

services:
  bot:
    build:
      context: bot
      dockerfile: prod.dockerfile

  database:
    image: postgres
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
